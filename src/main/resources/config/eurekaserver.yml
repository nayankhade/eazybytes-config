server:
  port: 8761

spring:
  application:
    name: eurekaserver

eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false

    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    
